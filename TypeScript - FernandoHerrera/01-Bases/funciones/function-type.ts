(() => {
    const addNumber = (a: number, b: number): number => a + b;

    const greet = (name: string): string => `Hola ${name}`;

    const saveTheWorld = (): string => `World saved successfully`;

    let myFunction: (a: number, b: number) => string;
})();
