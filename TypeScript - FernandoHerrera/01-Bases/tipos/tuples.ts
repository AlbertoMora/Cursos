(() => {
    const hero: [string, number] = ['Dr. Strange', 100];

    //hero[0] = 50; da error porque el primer valor de la tupla es un string
    //hero[1] = 'Dada' da error porque el segundo valor de la tupla es un number
})();
